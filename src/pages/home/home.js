import { BasicLayout } from "@/layouts";
import { Menu } from "@/components/layout";

export default function HomePage() {
    return (
      <BasicLayout>
        <Menu />
      </BasicLayout>
    );
}